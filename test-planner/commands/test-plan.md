---
description: Draft a structured test plan grounded in the code and publish it to Google Docs (or a local Markdown file)
---

Use the `test-plan` skill to create a test plan for the following feature/target.
Inspect the relevant code first, ask the user only for what can't be inferred
(scope boundaries, risk priorities, audience/depth), draft the plan from the
bundled template, show it for review, then publish to Google Docs if the Google
Drive connector is available or a local Markdown file otherwise.

Feature / target: $ARGUMENTS
